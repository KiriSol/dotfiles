- defaults:
    link:
      relink: true
      create: true
      force: true
      type: symlink

- clean:
    ["~", "~/.config", "~/.local/bin", "~/AppData/Local", "~/AppData/Roaming"]

- link:
    # Binaries
    ~/.local/bin/:
      path: bin/*
      glob: true

    # Configs
    ~/.config/git: config/git
    ~/.config/mise: config/mise-cli
    ~/AppData/Roaming/bat: config/bat
    ~/AppData/Roaming/yazi/config: config/yazi
    ~/AppData/Local/nvim: config/nvim

- create:
    - ~/Dev
    - ~/Tmp
