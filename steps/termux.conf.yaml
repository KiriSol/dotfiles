- defaults:
    link:
      relink: true
      create: true
      force: true
      type: symlink

- clean: ["~"]

- shell:
  - [rm -rf ~/.termux ~/.shortcuts]
  - [cp -r termux/termux ~/.termux]
  - [cp -r termux/shortcuts ~/.shortcuts]
